# Cho phép người dùng nhập vào một biến để lưu nhiệt độ (độ F)

# Input (nhập dữ liệu đầu vào)
F = float(input("Nhap do F: "))

# Process (xử lý, tính toán)
C = 5 / 9 * (F - 32)

# Output (Xuất dữ liệu đầu ra)
print("Do C:", C)